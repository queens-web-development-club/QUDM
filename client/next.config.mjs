/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com'],//added this just so i can use sample images on the frontpage
    }
};

export default nextConfig;
